pipeline{
    agent any
    stages{
        stage ('Build Code'){
            steps{
                sh '''
                  ls -lah
                  mvn --version
                '''
            }
        }
    }
}
